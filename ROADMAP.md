# Menu Plugin Modernization Roadmap

Target version: **1.3.0**  
Working branch: **modernize-1.3.0**

## Compatibility target

Menu 1.3.0 keeps one shared codebase for:

- Geeklog **2.1.1 through 2.2.2**
- PHP **5.6 through 8.1**
- MySQL / MariaDB
- single-site and multisite installations
- upgrades from Menu **1.2.5 through 1.2.8.1**

The modernization remains conservative: preserve existing menu data and legacy rendering while making the plugin safer, easier to maintain and usable by modern themes such as Eclipse.

---

## Current status — 2026-08-26

Menu 1.3.0 is now in **release-candidate / final validation** state. No new feature work is planned before the stable release unless a release-blocking defect is discovered.

### Implemented and covered by automated tests

- multisite-safe private storage in `{path_data}-menu/`;
- non-destructive migration from legacy `menu_data/`;
- site-specific public image paths;
- centralized upload validation and generated image filenames;
- POST-only state-changing administration operations with CSRF validation;
- centralized `menu.admin` mutation authorization;
- strict validation of menu, parent, order and element IDs;
- SQL string escaping and numeric casting on modernized persistence paths;
- database `AUTO_INCREMENT` for new elements instead of `MAX(id)+1`;
- hierarchy-safe menu cloning with old-ID → new-ID parent remapping;
- reliable menu deletion without orphaned `menu_config` or `menu_elements` rows;
- safe CSS color/image validation;
- safe URL rendering and dangerous-scheme rejection;
- compatibility helpers for Geeklog 2.1.1 through 2.2.2 and PHP 5.6 through 8.1;
- idempotent database index upgrade for `(menu_id, pid, element_order)`;
- three-query runtime menu loading independent of menu count;
- centralized runtime cache invalidation;
- cache traversal/symlink protection;
- SlickNav retained only as the narrow 1.3.0 legacy mobile compatibility layer;
- TableDnD 0.6 legacy ordering plus keyboard move controls;
- restored submenu creation/editing and sibling ordering;
- theme-facing resolved-tree API and theme presentation hand-off;
- native/theme administration preview;
- eight global Geeklog configuration switches;
- dead/obsolete administration and IE6 assets removed;
- permanent CI lint/tests on PHP 5.6 and PHP 8.1;
- installable `menu-1.3.0.zip` artifact generated by CI;
- static upgrade contract covering Menu 1.2.5 through 1.2.8.1.

### Validated on real Geeklog installations

The following paths have been exercised successfully during development on Geeklog 2.1.1 and/or 2.2.2 as applicable:

- menu administration loading;
- element creation/editing;
- submenu creation and parent selection;
- `Display After` ordering;
- drag-and-drop ordering;
- menu activation/deactivation;
- configuration/color save;
- image preview;
- menu cloning;
- menu/element deletion;
- public navigation rendering;
- footer rendering;
- authenticated/anonymous permission changes;
- fresh install/reinstall on Geeklog 2.1.1;
- uninstall/reinstall sequence on Geeklog 2.1.1;
- upgrade via the actual installable 1.3.0 archive;
- legacy upgrade paths from Menu 1.2.5 through 1.2.8.1 across the supported Geeklog test generations.

### Remaining release checks

Only final verification remains before 1.3.0 stable:

1. Manually exercise and save/reload all eight global configuration switches on the final Geeklog 2.1.1 and 2.2.2 test installations.
2. Perform a final Geeklog/PHP warning and error-log audit after those tests.
3. Confirm a final fresh install on Geeklog 2.2.2 if not already covered by the final test pass.

The upgrade matrix and installable package path are no longer release blockers.

SlickNav is **not** a release blocker for 1.3.0. Replacement/removal remains post-1.3.0 work unless a concrete security or compatibility defect is found.

---

## Completed modernization phases

### Phase 1 — Multisite-safe persistent storage

- [x] Centralize private storage through `MENU_dataDir()`.
- [x] Store plugin data in site-specific `{path_data}-menu/`.
- [x] Separate disposable cache from persistent custom CSS.
- [x] Migrate legacy `menu_data/` non-destructively and idempotently.
- [x] Never overwrite migrated destination files.
- [x] Keep public menu images site-specific.
- [x] Test multisite path isolation.

### Phase 2 — Security hardening

- [x] Protect state-changing operations with permissions, POST and CSRF validation.
- [x] Validate IDs, hierarchy, ordering and ownership before mutation.
- [x] Escape SQL strings and cast numeric values on modernized paths.
- [x] Replace active `MAX(id)+1` element creation with `AUTO_INCREMENT`.
- [x] Preserve hierarchy when cloning menus.
- [x] Escape labels and reject dangerous URL schemes.
- [x] Validate legacy CSS colors and image filenames.
- [x] Harden image uploads and deletion paths.

### Phase 3 — Geeklog 2.1.1 → 2.2.2 compatibility

- [x] Use one shared source tree.
- [x] Provide compatibility helpers for changed Geeklog APIs.
- [x] Preserve templates across the supported range.
- [x] Preserve Geeklog 2.2.x topic/userindex adaptations.
- [x] Validate legacy upgrade paths on supported test generations.

### Phase 4 — PHP 5.6 → 8.1 compatibility

- [x] Keep shared source syntax PHP 5.6 compatible.
- [x] Run permanent lint/tests under PHP 5.6 and PHP 8.1.
- [x] Avoid PHP 8-only APIs in shared code.
- [ ] Complete final runtime warning/error-log audit.

### Phase 5 — Database modernization

- [x] Preserve existing tables/data during upgrade.
- [x] Add idempotent `(menu_id, pid, element_order)` index support.
- [x] Follow Geeklog database charset/collation conventions.
- [x] Remove orphan-prone deletion logic.
- [x] Avoid new foreign-key constraints for compatibility.

### Phase 6 — Cache and performance

- [x] Separate disposable cache from persistent CSS.
- [x] Centralize cache invalidation after mutations.
- [x] Reduce runtime menu loading to three database queries independent of menu count.
- [x] Remove the unused HTML rendering cache path.
- [x] Retain safe CSS cache behavior.
- [x] Prevent cache cleanup traversal and symlink following.
- [x] Load SlickNav only where legacy rendering needs it.

### Phase 7 — Code cleanup and maintainability

- [x] Consolidate duplicated element classes.
- [x] Extract runtime loading, cache, security, mutation and view responsibilities.
- [x] Reduce `admin/index.php` to routing/page composition responsibilities.
- [x] Remove obsolete TableDnD generations and IE6-specific code.
- [x] Retain only tested legacy dependencies needed for compatibility.
- [x] Reject hierarchy cycles server-side.

**Phase 7 is closed for 1.3.0.** Further dependency replacement and cosmetic cleanup are post-release work.

### Phase 7.5 — Global plugin configuration

Implemented settings:

- [x] `enable_cache`
- [x] `accessibility_markup`
- [x] `external_link_protection`
- [x] `allow_php_elements`
- [x] `legacy_rendering`
- [x] `load_legacy_css`
- [x] `load_legacy_js`
- [x] `debug`

Also completed:

- [x] Remove obsolete Plugin Toolkit sample settings.
- [x] Preserve conservative defaults.
- [x] Provide idempotent 1.3.0 configuration upgrade plumbing.
- [x] Keep definitions compatible with Geeklog 2.1.1 through 2.2.2.
- [ ] Final manual switch-by-switch validation on both Geeklog generations.

### Phase 8 — Administration UX

- [x] Stabilize drag ordering on TableDnD 0.6.
- [x] Provide keyboard up/down ordering controls.
- [x] Normalize create/edit element type handling.
- [x] Restore submenu hierarchy editing.
- [x] Default new element ordering to the end of its sibling list.
- [x] Provide native/theme preview tabs.

### Phase 9 — Theme-facing API

- [x] Stable resolved-tree API.
- [x] Preserve hierarchy/order/permissions.
- [x] Resolve supported destinations before exposing them to themes.
- [x] Return presentation-neutral node data.
- [x] Keep legacy `MENU_getMenu()` behavior.

### Phase 10 — Theme integration

- [x] Keep menu structure/content responsibility in the plugin.
- [x] Allow modern themes to own HTML/CSS/JS presentation.
- [x] Expose navigation through the resolved-tree hand-off.
- [x] Preserve legacy rendering.
- [x] Support Eclipse consuming Menu navigation without duplicating destination-resolution logic.

---

## Phase 11 — Final release validation

Before publishing 1.3.0 stable:

- [x] Fresh install/reinstall on Geeklog 2.1.1.
- [ ] Final fresh install confirmation on Geeklog 2.2.2.
- [x] Upgrade from 1.2.5.
- [x] Upgrade from 1.2.6.
- [x] Upgrade from 1.2.7.
- [x] Upgrade from 1.2.8.
- [x] Upgrade from 1.2.8.1.
- [x] Upgrade through the actual generated `menu-1.3.0.zip` package.
- [x] Automated migration idempotence coverage.
- [x] Automated multisite isolation coverage.
- [x] Automated cache/data separation coverage.
- [x] Verify uninstall/reinstall behavior on Geeklog 2.1.1.
- [x] CI regression matrix on PHP 5.6 and PHP 8.1.
- [x] Validate automated release-package generation.
- [ ] Final switch-by-switch configuration pass.
- [ ] Final runtime warning/error-log audit.

## Post-1.3.0 candidates

- replace/remove SlickNav after legacy rendering stabilization;
- JSON import/export;
- multisite clone/export/import workflows;
- optional SVG/emoji/CSS-class icons;
- additional safe link attributes;
- richer display conditions;
- reusable/inherited menu structures;
- continued responsive/accessibility polish.

## Compatibility principle

Do not maintain separate source trees for old and new Geeklog releases. Prefer small compatibility helpers and runtime capability checks so one Menu release supports the declared range.

## Data-safety principle

Existing user data takes precedence over cleanup convenience. Upgrade routines must remain non-destructive, repeatable and conservative. Legacy files/directories may remain after successful migration and can be documented for optional manual cleanup.
